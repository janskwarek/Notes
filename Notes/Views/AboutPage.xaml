<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Notes.Views.AboutPage"
             xmlns:models="clr-namespace:Notes.Models"
             Title="About"
             x:DataType="models:About">
    <ContentPage.BindingContext>
        <models:About></models:About>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <VerticalStackLayout Spacing="10" Margin="10">
            <HorizontalStackLayout Spacing="10">
                <Image Source="{Binding LogoSource}" SemanticProperties.Description="{Binding LogoDescription}" HeightRequest="64"> </Image>
                <Label FontSize="22" FontAttributes="Bold" Text="{Binding Title}" VerticalOptions="End">
                </Label>
                <Label FontSize="22" Text="{Binding Version}" VerticalOptions="End"></Label>
            </HorizontalStackLayout>
            <Label Text="{Binding Message}"></Label>
            <Button Text="{Binding MoreInfoText}" Clicked="LernMore_Clicks"></Button>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>